
// import React from 'react';

// const Cart = ({ cartItems, total, onCheckout }) => {
//   return (
//     <aside className="w-1/4 bg-white p-6 shadow-lg">
//       <h2 className="text-2xl font-semibold">Your Cart</h2>
//       <div className="mt-4">
//         {cartItems.length === 0 ? (
//           <p>Your cart is empty.</p>
//         ) : (
//           cartItems.map((item, index) => (
//             <div key={index} className="flex justify-between mt-2">
//               <p>{item.name}</p>
//               <p>${item.price}</p>
//             </div>
//           ))
//         )}
//       </div>
//       <div className="mt-4 flex justify-between items-center">
//         <p className="font-semibold text-xl">Total:</p>
//         <p className="text-xl">${total}</p>
//       </div>
//       <button 
//         className="w-full bg-[#ffd700] text-white p-2 mt-4 rounded-lg hover:bg-[#f5deb3] transition-colors"
//         onClick={onCheckout}
//       >
//         Proceed to Checkout
//       </button>
//     </aside>
//   );
// };

// export default Cart;
import React from 'react'

function Cart() {
  return (
    <div className=''>
    </div>
  )
}

export default Cart;