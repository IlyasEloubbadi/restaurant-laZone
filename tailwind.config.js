module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Ajoute les fichiers React
  darkMode: 'class', // Enable dark mode via class strategy
  theme: {
    extend: {
      // You can extend the default theme if needed
    },
  },
  theme: {
    extend: {},
  },
  plugins: [],
};
